# Middleware 执行流程

**本文档已过期** — 展示的是旧版 8-13 步 middleware 链（"Phase 1/Phase 2"），与当前实际的 26 步链不符。当前权威文档见 [architecture/agent-runtime.md](architecture/agent-runtime.md)（含完整 26 项 middleware 列表，`build_lead_runtime_middlewares` + `build_middlewares`）以及 `backend/CLAUDE.md` → `## Architecture` → Agent System, Middleware Chain, Subagents。
