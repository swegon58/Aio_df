# Memory Index

- [graphify usage](graphify-usage.md) — graphify installed + built graph; lean-ctx blocks `graphify` binary → run via uv python module; query via `graphify query`
- [Aio_df 2026-07-22 session state](aio-df-2026-07-22-session-state.md) — auth fix + Pro-mode model override done, uncommitted changes list, what's next
- [Aio_df mode→model override](aio-df-mode-model-override.md) — chat mode had no model binding by default; fixed via `mode_model_overrides`, don't touch `models[0]` for mode-scoped asks
- [z.ai model availability](zai-model-availability.md) — how to test which GLM models a z.ai key can use; FlashX 429 gap on SwegOn's account
